<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apartmentmanagement.features.authencation.mapper.AccountMapper">

    <insert id="insertAccount">
        INSERT INTO Account(username, password, phone, email)
        VALUES (#{username}, #{password}, #{phone}, #{email} )
    </insert>

    <select id="findAccountByUsername">
        SELECT * FROM Account
        WHERE username = #{username}
        AND status = 'ACTIVE'
    </select>

    <select id="validationAccount">
        SELECT * FROM Account
        Where email = #{email}
           OR phone = #{phone}
           OR username = #{username}
    </select>

    <update id="softDeleteAccount">
        UPDATE Account
        SET status = 'DELETE'
        WHERE id = #{accountId}
    </update>

    <update id="softBanedAccount">
        UPDATE Account
        SET status = 'BANED'
        WHERE id = #{accountId}
    </update>

    <update id="unBanedAccount">
        UPDATE Account
        SET status = 'ACTIVE'
        WHERE id = #{accountId}
    </update>

</mapper>